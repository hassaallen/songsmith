---
deployment:
  tasks:
    # Copies the web-served files from the repo's public/ folder into the
    # subdomain's document root. (Trailing slash matters.)
    # Confirm this path matches what cPanel showed when the subdomain was created.
    - export DEPLOYPATH=/home/hassaall/songwriting.hassaallen.net/
    - /bin/cp -R public/. $DEPLOYPATH
