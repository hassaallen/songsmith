# Force HTTPS — the session cookie is Secure-only, so plain http produces
# phantom logins (login appears to work, nothing else does).
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
